# SQLite is used by default and stores data in db.sqlite3 at the project root.
# No database connection settings are required for local development.

# Email settings
EMAIL_HOST_USER=your-email@example.com
EMAIL_HOST_PASSWORD=your-email-password
MENTOR_EMAIL=mentor@example.com
